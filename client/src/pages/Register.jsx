import AuthForm from '../components/AuthForm';

const Register = () => {
  return (
    <div className="min-h-screen flex items-center justify-center retro-texture" style={{ backgroundColor: 'var(--retro-black)' }}>
      <AuthForm />
    </div>
  );
};

export default Register;